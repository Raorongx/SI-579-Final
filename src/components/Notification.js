import React from 'react';

const Notification = () => {
    // Notification logic

    return (
        <div>
            {/* Notification UI */}
        </div>
    );
};

export default Notification;
